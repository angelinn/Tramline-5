<ContentDialog
    x:Class="TramlineFive.Views.Dialogs.DirectionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TramlineFive.Views.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Избор на разписание"
    PrimaryButtonText="Напред" 
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick" >

    <ContentDialog.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource BlueButton}" />
    </ContentDialog.Resources>

    <StackPanel Margin="0,20,0,0">
        <ComboBox Name="cbDirections" ItemsSource="{Binding Directions}" DisplayMemberPath="Name" PlaceholderText="Избор на маршрут" SelectionChanged="cbDirections_SelectionChanged" />
        <ComboBox Name="cbDays" ItemsSource="{Binding Days}" DisplayMemberPath="Type" PlaceholderText="Избор на ден" IsEnabled="False" Margin="0,10,0,0" SelectionChanged="cbDays_SelectionChanged" />
    </StackPanel>
</ContentDialog>
