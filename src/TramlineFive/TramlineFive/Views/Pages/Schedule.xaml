<Page
    x:Class="TramlineFive.Views.Pages.Schedule"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TramlineFive"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.TopAppBar>
        <CommandBar Background="{ThemeResource AccentColour}">
            <CommandBar.Content>
                <StackPanel Orientation="Horizontal">
                    <Button Name="btnBack" FontFamily="Segoe MDL2 Assets" Content="&#xE830;" Padding="15" FontSize="20" FontWeight="Bold" Click="btnBack_Click" />
                    <TextBlock Name="txtTitle" FontWeight="Bold" Margin="12,14,0,0" Foreground="White"/>
                </StackPanel>
            </CommandBar.Content>
        </CommandBar>
    </Page.TopAppBar>

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="20">
        <ComboBox ItemsSource="{Binding Stops}" PlaceholderText="Изберете спирка" DisplayMemberPath="Name" SelectionChanged="ComboBox_SelectionChanged" />
        <TextBlock Name="txtTimings" TextWrapping="Wrap" Margin="0,20,0,0" />
    </StackPanel>
</Page>
